import 'package:flutter/material.dart';

/// Placeholder for the Log Dose tab.
/// Will have a form to record doses in Milestone 2.
class LogScreen extends StatelessWidget {
  const LogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Log Dose — coming soon'),
    );
  }
}
